# Development Environment Variables
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_VERSION=1.0.0-dev
# Add other development variables as needed
# SENTRY_DSN should be set in the Vercel dashboard for production
# For development, you can set a SENTRY_DSN if you want to capture dev errors, but it's not required